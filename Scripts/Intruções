Boa tarde cavalheiros, para usar o codigo primeiro vc precisa do pycryptodome e do crypto, ent só mete um pip maroto e instala essa budega!!!

pip install pycryptodome

pip install cryptodome

Com tudo instalado e pronto é só rodar o codigo em algum terminal, como cmd ou mesmo dentro do vscode.

